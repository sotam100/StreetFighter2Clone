{
  "name": "streetfighter2clone",
  "services": [
    {
      "name": "web",
      "type": "docker",
      "dockerfile": "Dockerfile",
      "runCommand": "nginx -g 'daemon off;'",
      "port": 80
    }
  ],
  "notes": "Serves pre-built WebGL from Build/WebGL directory via nginx."
}
